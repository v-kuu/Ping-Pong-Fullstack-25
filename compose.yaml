services:
  astro:
    build: ./site
    container_name: astro
    ports:
      - "3000:3000"
      - "3001:3001"
      - "3002:3002"
    restart: always
    env_file:
      - .env
    volumes:
      - ./data/db:/app/.astro
      - ./data/avatars:/app/dist/client/avatars
      - ./certs:/certs:ro

